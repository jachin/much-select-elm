---
name: devbox-tasks
description: Run devbox tasks
disable-model-invocation: false
---

Look at the devbox.json file. It defines tasks that can be run easily.

If there's something you are doing over and over again suggest that we add it as a devbox task.
