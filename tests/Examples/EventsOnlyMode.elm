module Examples.EventsOnlyMode exposing (suite)

import Expect
import Test exposing (Test, describe, test)


suite : Test
suite =
    describe "Example: events-only-mode"
        [ test "TODO: suppresses internal value rendering while still emitting events" <|
            \_ ->
                Expect.pass
        ]
